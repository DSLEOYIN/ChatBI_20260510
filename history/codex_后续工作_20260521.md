# Codex 后续工作记录 2026-05-21

> 同步时间：2026-05-21  
> 基准 PRD：`docs/ChatBI_PRD_草案.md`  
> UI 基准：`前端原型/index_v3_dynamic.html`  
> 关联计划：`docs/2026-05-20_ChatBI后续开发计划.md`  
> 关联交接：`history/2026-05-20_chatbi_react_mock_handoff.md`、`history/2026-05-20_chatbi_interactive_parity_v3_history.md`、`history/2026-05-20_chatbi_hotfixes_and_stage2_prep.md`、`history/update_history_v2.md`、`history/2026-05-21_drawer_layout_bugfix.md`

## 1. 今日接续背景

当前项目仍处于 ChatBI Workspace 2.0 第一阶段：以前端原型 `前端原型/index_v3_dynamic.html` 为唯一视觉与交互基准，先通过 React + FastAPI mock 跑通完整业务链路，再逐步替换为真实 MySQL、Dify、Tavily MCP、LangGraph Agent 与权限体系。

本阶段核心目标不是重新设计 UI，而是把原型中已经确认的体验完整迁移到 React 项目：

1. 悬浮球、精简窗口、全屏工作区、历史会话抽屉必须与原型对齐。
2. 左侧展示用户问题、Agent 可理解执行链、SQL/工具复核信息；最终业务答案统一在右侧业务画布输出。
3. 右侧画布由结构化 payload 驱动，支持最终回答、KPI、风险预警、图表、表格、结论建议、搜索结果、指标定义等组件。
4. 第一阶段后端接口全部 mock，重点保证会话、SSE 流式问答、执行链、画布、下载、数据资产、Skill 注册等流程稳定。
5. 用户可见的是“可理解的执行链/审计链”，不是完整内部推理。

当前本机验证端口：

- 前端：`http://127.0.0.1:5174/`
- 后端：`http://127.0.0.1:8001/`
- 前端环境变量：`VITE_API_URL=http://127.0.0.1:8001/api`

说明：`5173` 端口当前被其他项目占用，因此本轮验证临时使用 `5174`。

## 2. 已完成工作总账

### 2.1 工程骨架与 MVP mock 链路

已完成内容：

1. 建立 React 18 + Vite + ECharts 前端应用骨架。
2. 建立 FastAPI mock 后端服务。
3. 跑通 SSE 流式问答接口。
4. 建立基础会话历史持久化。
5. 建立基础 Skill 注册表与数据资产接口。
6. 建立结构化右侧画布组件渲染能力。
7. 建立明细 CSV 下载能力。
8. 补入 SQL 校验异常与自愈演示节点。

涉及主要文件：

- `client/src/main.jsx`
- `client/src/styles.css`
- `client/src/services/api.js`
- `server/app/main.py`
- `server/app/api/routes.py`
- `server/app/mock/engine.py`
- `server/app/mock/catalog.py`
- `server/app/storage.py`
- `server/app/models/schemas.py`

### 2.2 原型方向纠偏与 React 高保真复刻

已完成内容：

1. 明确 `前端原型/index_v3_dynamic.html` 是唯一视觉和交互基准。
2. 移除早期偏离原型的自创 UI 方向。
3. 移除“专注报告”等原型外可见文案。
4. 将业务最终回答从左侧聊天流迁移到右侧业务画布。
5. 左侧聚焦用户问题、Agent 执行链、SQL/工具复核、审计信息。
6. 复制并使用原型资产到 `client/public/assets/`。
7. 卡片历史 / 可访问数据改为工作区右侧滑入式浮层。

当前产品口径：

- 原型浅色商务风格就是第一阶段上线标准。
- SQL 默认折叠。
- SQL 出错自愈演示第一版可以保留。
- Demo 数据使用 mock 合理数。
- 会话历史必须持久化。
- 用户偏好记忆第一版 mock 即可。

### 2.3 悬浮球拖拽、吸附与工作区随动定位

已完成内容：

1. 新增拖拽边界限制，悬浮球只能在可视区域内移动。
2. 新增边缘吸附算法，释放后自动吸附至最近左右边缘。
3. 区分点击与拖拽，超过 `3px` 位移不触发工作区展开/收回。
4. 实现精简工作区根据悬浮球象限随动弹出。
5. 动态写入 `--workspace-top`、`--workspace-right`、`--workspace-width`、`--workspace-height` 根 CSS 变量。
6. 卡片历史与可访问数据抽屉改用 CSS 变量随工作区定位，滑入动画更稳定。

涉及文件：

- `client/src/main.jsx`
- `client/src/styles.css`

### 2.4 全屏切换、执行链可读性与第二阶段准备

已完成内容：

1. 修复全屏最大化时被 inline 坐标覆盖的问题。
2. 全屏时强制复位 `left/top/right/bottom/width/height/transformOrigin`，保证工作区铺满可视区域。
3. 退出全屏后重新调用悬浮球邻近定位，保持精简态体验稳定。
4. 移除前端废弃的 Skill 注册表状态和多余轮询逻辑。
5. 放大执行链 Input/Output 代码块字体与高度。
6. 移除执行链详情区域双重滚动条。
7. 全屏状态下执行链列表可用高度扩展至更适合复核的空间。

涉及文件：

- `client/src/main.jsx`
- `client/src/styles.css`

### 2.5 第二阶段：流式执行链与 SQL 自愈展示

已完成内容：

1. 实现 `highlightJson` 轻量 JSON 语法高亮。
2. 对 Input/Output 中 JSON 的 key、string、number、boolean、null 做分色展示。
3. 实现业务答案生成中的打字机闪烁光标。
4. 将 `activeStep` 提升至 App 层。
5. SSE 接收 `warning` 或 `error` 节点时，自动展开对应执行链详情。
6. SQL 沙盒校验警告、自愈重写、修复结果可以在执行链中自动聚焦展示。

涉及文件：

- `client/src/main.jsx`
- `client/src/styles.css`

### 2.6 历史会话置顶、删除与输入区细节优化

已完成内容：

1. 提问输入框支持自适应高度，最小约 `34px`，最大约 `100px`。
2. 后端新增会话置顶持久化能力。
3. 后端新增 `POST /api/conversations/{conversation_id}/pin` 置顶接口。
4. 前端新增 `pinConversation` API 调用。
5. 历史列表项加入置顶、删除动作，并阻止按钮点击冒泡。
6. 新建对话按钮完成视觉优化。
7. 明细下载按钮改为 FontAwesome Excel 图标，并适配精简态折叠。

涉及文件：

- `client/src/main.jsx`
- `client/src/styles.css`
- `client/src/services/api.js`
- `server/app/api/routes.py`
- `server/app/storage.py`

### 2.7 历史会话恢复能力补强

已完成内容：

1. 新增 `getAutoActiveStep`，历史恢复时自动定位第一条 `warning` 或 `error` 执行链节点。
2. 新增 `makeCanvasCard`，统一生成右侧“卡片历史”条目。
3. 点击历史会话后同步恢复：
   - 用户消息列表
   - 最后一条 assistant 结果的执行链
   - SQL 数据状态
   - 右侧业务画布
   - 当前会话内可用的卡片历史
   - warning/error 节点展开状态
4. 历史恢复时清理残留打字机定时器。
5. 历史恢复时关闭历史抽屉与右侧抽屉，避免 UI 状态串场。

涉及文件：

- `client/src/main.jsx`

浏览器验证结果：

- 提问“诊断中东公司 GS8 车型本月的库存异常并给出建议”后可正常生成。
- 从历史会话点回后状态保持一致：
  - 执行链节点数：正常恢复
  - warning 节点自动展开：是
  - 右侧业务画布卡片数：6
  - 卡片历史条目数：1
  - 打字机状态残留：无

### 2.8 历史对话列表布局修复

已完成内容：

1. 修复 `.drawerList` 缺少 flex 约束导致历史项挤在一起的问题。
2. `.drawerList` 改为纵向 flex，并加入 `flex: 1` 与 `min-height: 0`，保证滚动生效。
3. 修复历史标题 `text-overflow: ellipsis` 在 `b` 标签内失效的问题。
4. 新增 `.drawerItemTitle`，用具名 `span` 承载标题截断。

涉及文件：

- `client/src/styles.css`
- `client/src/main.jsx`

验收点：

- 历史会话项正常分行排列。
- 历史数量超过可视区域时列表可以滚动。
- 超长标题显示省略号，不撑开卡片宽度。

### 2.9 本地开发 CORS 补强

已完成内容：

1. 后端 CORS 白名单新增：
   - `http://localhost:5174`
   - `http://127.0.0.1:5174`

原因：

- 当前机器 `5173` 端口被其他项目占用，ChatBI 前端需要临时运行在 `5174` 完成本地验证。

涉及文件：

- `server/app/main.py`

### 2.10 移除独立“查看 SQL”按钮

已完成内容：

1. 移除 `ExecutionPanel` 底部独立 SQL 折叠面板。
2. 移除“查看 SQL / 折叠 SQL”按钮。
3. 删除对应 `.sqlBox` 与全局 `pre` 样式。
4. 保留内部 `sql` 状态保存与历史恢复数据，不破坏后续审计链路。

产品原因：

- 执行链节点内已经通过 Input / Output 展示 SQL 生成、SQL 校验异常、SQL 自愈修复等关键信息。
- 独立 SQL 按钮会形成重复入口，当前阶段应让用户主要通过执行链逐节点复核。

涉及文件：

- `client/src/main.jsx`
- `client/src/styles.css`

### 2.11 P0 历史对话列表二次修复与验收

用户反馈：

- 历史对话列表仍存在挤压、滚动、标题溢出问题，上一轮 2.8 修复未覆盖窄窗口、大量历史、置顶项和超长标题组合场景。

已完成内容：

1. `.historyDrawer` 增加 `overflow: hidden`，避免抽屉内部内容外溢。
2. `.historyDrawer header` 与 `.drawerNew` 增加 `flex: 0 0 auto`，防止头部和新建按钮被列表挤压。
3. `.drawerList` 改为 `flex: 1 1 auto`，并补充 `align-items: stretch`、`overflow-x: hidden`、`overscroll-behavior: contain`。
4. `.drawerItem` 增加 `flex: 0 0 auto`、`width: 100%`、`min-width: 0`、`min-height: 68px`，确保历史项在纵向滚动列表内不再被压扁。
5. `.drawerItemContent`、`.drawerItemContent b`、`.drawerItemTitle`、`small` 补齐完整的 `min-width: 0` / `overflow` / `text-overflow` 链路，保证标题和时间都不会撑开卡片。
6. `.drawerItemActions` 增加 `flex: 0 0 auto`，避免置顶/删除按钮参与标题区域压缩计算。

涉及文件：

- `client/src/styles.css`

浏览器验收结果：

- 临时插入 24 条 QA 历史记录，加上现有历史共 54 条进行压力验证；验收后已删除 QA 临时记录。
- `.drawerList` 实测 `clientHeight: 476`、`scrollHeight: 4012`、`overflow-y: auto`，滚动生效。
- 前 8 条可见历史项高度均为 `68px`，`flex-shrink: 0`，未发生纵向挤压。
- 超长标题 `scrollWidth > clientWidth` 时保持 `white-space: nowrap` 与省略号截断，无横向溢出。
- QA 截图已归档：`qa/history_drawer_layout_fix_20260521.png`。

## 3. 验证记录

已执行：

```bash
cd client
npm run build
```

结果：

- Vite 构建通过。
- 仅存在大 chunk 常规提示，不影响功能。

已执行：

```bash
cd server
python -m compileall app
```

结果：

- Python 编译检查通过。

已进行浏览器验证：

1. 悬浮球拖拽、边缘吸附、点击展开。
2. 精简工作区随悬浮球象限弹出。
3. 卡片历史 / 可访问数据抽屉随工作区边缘滑出。
4. SQL 自愈 warning 节点自动展开。
5. 历史会话恢复右侧画布与卡片历史。
6. 历史会话列表分行、滚动、标题省略。
7. 历史会话列表二次压力验证：54 条历史、超长标题、置顶项、按钮显隐场景下无挤压、可滚动、无标题横向溢出。

QA 截图：

- `qa/history_drawer_layout_fix_20260521.png`

## 4. 当前运行方式

后端：

```bash
cd server
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001
```

前端：

```bash
cd client
$env:VITE_API_URL='http://127.0.0.1:8001/api'
npm run dev -- --host 127.0.0.1 --port 5174
```

访问：

- `http://127.0.0.1:5174/`
- `http://127.0.0.1:8001/api/health`

## 5. 当前 mock 接口完成度

| PRD mock 能力 | 当前接口 / 实现 | 当前判断 | 后续动作 |
| --- | --- | --- | --- |
| 新建会话 | `POST /api/conversations` | 基础可用 | 对齐原型新会话行为与状态清理 |
| 历史列表 | `GET /api/conversations` | 基础可用 | 补历史搜索、排序、置顶展示验收 |
| 历史详情 | `GET /api/conversations/{id}` | 已增强 | 持续验证右侧画布、卡片历史、warning 展开 |
| 删除历史 | `DELETE /api/conversations/{id}` | 基础可用 | 继续复刻原型二次确认弹窗 |
| 置顶历史 | `POST /api/conversations/{id}/pin` | 已补入 | 验证置顶排序与历史恢复组合状态 |
| 流式问答 | `POST /api/chat/stream` | 可跑通 | 标准化 SSE 事件协议 |
| 非流式问答 | `POST /api/chat` | 调试可用 | 保留为调试接口 |
| 画布 payload | chat result 内嵌 | 可用但未定稿 | 文档化组件协议，必要时拆独立接口 |
| 明细下载 | `GET /api/downloads/mock-detail.csv` | CSV 可用 | 明确 CSV/xlsx 口径，优化下载提示 |
| 联网搜索 | chat mock 固定结果 | 初步可演示 | 拆出 mock 搜索结构，后续接 Tavily MCP |
| 数据资产 | `GET /api/config/data-assets` | 表级简版 | 补字段、别名、指标口径、搜索字段 |
| Skill 注册 | `GET /api/config/skills` | 基础列表 | 补适用场景、输入输出、MCP、降级策略 |

## 6. 后续计划任务

### P0：用户验收反馈闭环

目标：先让真实可见体验稳定，不急于扩功能。

本轮状态：

- 历史对话列表挤压、滚动、标题溢出问题已二次修复并通过浏览器压力验证。
- 下一轮用户可继续重点验收历史恢复、warning 自动展开、右侧画布恢复、卡片历史恢复与 SQL/工具复核清晰度。

任务：

- [x] 请用户重点验收历史会话恢复、warning 节点自动展开、右侧画布恢复、卡片历史恢复。
- [x] 验收历史对话列表是否仍存在挤压、滚动、标题溢出问题。
- [x] 验收移除独立 SQL 按钮后，执行链节点内的 SQL/工具复核信息是否足够清晰。
- [x] 若发现视觉或交互偏差，优先修复真实可见问题。

### P0：继续做原型高保真对齐

目标：React 页面肉眼效果与 `前端原型/index_v3_dynamic.html` 高度一致。

任务：

- [x] 对齐执行链卡片高度、间距、滚动节奏、节点展开动画。
- [x] 对齐右侧“卡片历史 / 可访问数据”抽屉尺寸、贴边位置、进入/退出动画。
- [x] 对齐 Canvas 顶部按钮图标、字号、精简模式折叠表现。
- [x] 对齐欢迎推荐卡、输入区、联网搜索开关、发送按钮状态。
- [x] 对齐悬浮球拖拽吸附在移动端和窄屏下的表现。
- [x] 每次 UI 修复后截图归档到 `qa/`，并与原型截图对照。

验收：

- [x] 精简窗口下卡片历史与可访问数据从工作区右侧跳出，不被边框裁切。
- [x] 全屏状态下两个浮层位置仍与原型一致。
- [x] 首页欢迎区、执行链、画布卡片、输入区、按钮图标与原型保持一致。

### P1：执行链 Input / Output 信息密度优化

目标：让 SQL、工具调用、修复原因、结果摘要在执行链内更容易复核。

任务：

- [x] 统一 step payload 字段命名。
- [x] 为 SQL Generator / SQL Sandbox / SQL Repair Skill 增加更明确字段：
   - `generatedSql`
   - `failedSql`
   - `repairReason`
   - `repairedSql`
   - `validationRules`
- [x] 增强节点标题、工具名、耗时、状态、输入、输出、错误、自愈记录。
- [x] 控制信息展示密度，避免业务用户看到过多技术噪声。

### P1：Mock API 补全与契约稳定

目标：覆盖 PRD 6.6 定义的第一阶段 mock 能力。

任务：

- [x] 定义 SSE 事件协议：`conversation_id`、`step`、`sql`、`answer`、`canvas`、`done`、`error`。已通过 `GET /api/config/stream-contract` 暴露，并在 SSE envelope 中补充 `sequence`、`emitted_at`。
- [x] 增强数据资产接口，返回表、字段、中文名、示例值、业务别名、指标口径。已通过 `GET /api/config/data-assets` 暴露。
- [x] 增强 Skill 注册接口，返回适用场景、触发条件、输入输出结构、MCP 工具、失败降级策略。已通过 `GET /api/config/skills` 暴露。
- [x] 文档化画布 payload 协议，至少覆盖：
   - `answer`
   - `kpi`
   - `chart`
   - `table`
   - `risk`
   - `definition`
   - `search_results`
   - `insight`
- [x] 下载接口改为 xlsx，或明确第一版继续 CSV，同时前端提示文案与原型一致。当前第一版明确继续 CSV，契约见 `GET /api/config/download-contract`，下载入口为 `GET /api/downloads/mock-detail.csv`。
- [x] 联网搜索 mock 拆出固定结果结构，为后续 Tavily MCP 替换留接口。当前结构为 `MOCK_SEARCH_RESULTS`，接口为 `GET /api/mock/search`。

验收：

- [x] PRD 6.6 七类 mock 能力均有接口或明确 payload。汇总契约见 `GET /api/config/mock-contract`。
- [x] 欢迎卡黄金问题均能稳定生成执行链、SQL、最终回答和右侧画布。
- [x] 刷新/恢复历史后右侧画布不丢失。

### P1：黄金演示问题打磨

目标：先把 Demo 体验做稳，而不是泛化所有问题。

建议固定 4 条黄金问题：

1. `诊断中东公司 GS8 车型本月的库存异常并给出建议`
2. `分析本月中东公司车型排产与实际到货差额`
3. `GS8 当前库存是多少，是否超过安全线`
4. `什么是库存周转天数，计算公式是什么`

每条问题都要具备：

- [x] 明确意图识别。
- [x] 不同执行链节点组合。
- [x] 不同右侧画布组件组合。
- [x] 合理 mock 数据。
- [x] 可恢复历史。
- [x] 可下载明细。
- [x] 截图归档到 `qa/`。

### P2：真实 API 替换准备

当前 mock 已具备稳定 UI 骨架，真实接口替换建议顺序：

- [x] 会话接口：SQLite mock 存储升级为可迁移存储结构。已新增 `schema_migrations`、版本化 migration、`CHATBI_DB_PATH` 覆盖和 `GET /api/config/storage` 验收接口。
- [x] 数据资产 / 字段口径：从 mock 配置升级为 YAML/JSON 或数据库配置。当前采用 JSON 文件 `server/config/data_assets.json`，支持 `CHATBI_DATA_ASSETS_PATH` 覆盖，接口仍为 `GET /api/config/data-assets`。
- [ ] Dify 知识库检索：先封装 Retrieve API，再封装为 MCP。
- [ ] MySQL 查询：只读 SELECT，增加 SQL 安全校验与 allowed tables 过滤。
- [ ] Tavily MCP 搜索：替换固定 mock 搜索结果。
- [ ] LangGraph Agent：替换当前硬编码 intent/mock engine。
- [ ] 权限系统：MVP 后补充用户角色、可访问场景、可访问表、可下载字段。

### P2：后台配置设计补齐

PRD 中后台配置尚未定型，需要后续设计：

- [ ] 场景配置：场景名、描述、可用表、指标口径、别名词典。
- [ ] 表与字段配置：表名、中文名、字段名、字段含义、示例值、是否可查询。
- [ ] Skill 配置：适用场景、执行说明、可调用 MCP、输入输出结构。
- [ ] MCP 工具配置：连接方式、参数说明、返回结构、失败降级策略。
- [ ] 画布模板配置：不同意图、不同场景下的组件组合。
- [ ] 下载配置：可下载结果、下载字段、文件命名规则。
- [ ] 搜索配置：是否允许联网搜索、搜索深度、结果数量、引用展示。
- [ ] 权限配置：MVP 暂不实现，但需预留设计。

待确认：

- 后台配置第一版放在独立管理页、数据库配置表、YAML/JSON 文件，还是先硬编码 mock 配置。

## 7. PRD 待确认问题同步

以下问题需要产品或业务继续确认：

1. HTML 画布 payload 标准：组件 JSON、完整 HTML 片段，还是 Markdown + chart/table schema。
2. 明细下载第一版是前端生成 Excel，还是后端生成文件。
3. SQL 是否对普通业务用户可见，默认折叠还是按权限展开。
4. 指标口径由 Dify 知识库维护，还是需要后台管理页面。
5. 内网环境是否完全物理隔离，是否允许 CDN、外部模型或 Tavily。
6. 演示 Demo 的三到四条黄金问题最终采用哪几条。
7. 真实 MySQL 接入时，是否沿用 YAML 中 8 张表作为第一批可查询数据资产。
8. Skill 配置第一版采用代码注释、数据库配置、YAML/JSON 文件，还是后台页面。

## 8. 注意事项

1. 当前工作树中存在既有未提交改动与未跟踪文件，继续开发时不要误删或覆盖用户已有改动。
2. 当前可见未提交项包括：
   - `client/package-lock.json`
   - `client/src/main.jsx`
   - `client/src/styles.css`
   - `server/app/main.py`
   - `.claude/`
   - `my-mcp-tool/agent-engineer-assistant.zip`
   - `my-mcp-tool/mcp-server-dify/`
3. 后续所有中文交接文档统一使用 UTF-8 保存，避免早期文档出现编码显示问题。
4. 继续开发时先对照 PRD 与原型，不要新增原型外可见文案或布局结构。
5. UI 修复优先处理用户真实可见问题；接口替换和架构升级放在 mock 契约稳定之后。

## 9. 2026-05-21 P0 状态同步

### P0：用户验收反馈闭环

状态：已关闭。

说明：历史会话列表挤压、滚动、标题溢出问题已由用户确认修复，本轮不再继续占用 P0 修复通道。

### P0：继续做原型高保真对齐

本轮已启动第一批对齐：

1. 执行链节点样式向原型 trace timeline 靠齐：节点尺寸、间距、状态色、连线、hover/open 背景与详情展开动效已调整。
2. 修复执行链 warning 节点自动展开后 I/O 详情不撑高、压住后续节点的问题。
3. 右侧画布卡片从双列 grid 收敛为原型纵向卡片流，统一卡片圆角、阴影、KPI 密度、图表高度与滚动节奏。
4. 补齐 980px 与 760px 两档响应式规则，使精简窗口、移动端窄屏、卡片历史/可访问数据抽屉位置更接近原型。
5. 已完成浏览器验证与截图归档。

验证记录：

- `client` 执行 `npm run build` 通过，仅保留 Vite 大 chunk 常规提示。
- 浏览器验证：执行链 8 个节点正常渲染，warning 节点自动展开，右侧生成 6 张业务卡片，卡片流可滚动。
- QA 截图：
  - `qa/chatbi_p0_prototype_parity_initial_20260521.png`
  - `qa/chatbi_p0_prototype_parity_answer_20260521.png`
  - `qa/chatbi_p0_prototype_parity_card_pool_20260521.png`
  - `qa/chatbi_p0_prototype_parity_catalog_20260521.png`
  - `qa/chatbi_p0_prototype_parity_mobile_20260521.png`

## 10. 2026-05-21 执行链展开文字溢出修复

用户反馈：

- Agent 执行链节点点开后，部分标题、描述、工具名或 Input / Output 详情文本会撑出节点容器。

修复内容：

1. `.stepList` 补充 `overflow-x: hidden`，避免执行链列表出现横向溢出。
2. `.step`、`.stepBody`、`.stepTop`、`.stepDetail`、`.stepDetail > span`、`.stepDetail code` 补齐 `min-width: 0` / `max-width: 100%` / `overflow` 约束。
3. `.stepDesc` 与 `.stepDetail > span` 增加 `overflow-wrap: anywhere`，长中文、英文字段、工具名、SQL 片段与 JSON 字符串会在容器内换行。
4. `.stepDetail code` 保留内部滚动能力，同时补充 `overflow-wrap: anywhere`，避免超长 SQL/JSON 字符串撑开执行链卡片。
5. `.step b` 改为单行省略，保证超长节点标题不会挤压耗时标签或撑宽卡片。
6. `.executionHead span` 改为单行省略，避免 Skill 摘要过长撑开执行链头部。

涉及文件：

- `client/src/styles.css`

验证记录：

- `client` 执行 `npm run build` 通过，仅保留 Vite 大 chunk 常规提示。
- 浏览器验证：触发“诊断中东公司 GS8 车型本月的库存异常并给出建议”后生成 8 个执行链节点。
- 浏览器验证：warning 节点自动展开后，`.stepList` 的 `clientWidth` 与 `scrollWidth` 均为 `361px`，无横向溢出。
- 浏览器验证：逐个点开 8 个执行链节点，`.step`、`.stepBody`、`.stepTop`、`.stepDesc`、`.stepDetail`、`.stepDetail > span`、`.stepDetail code` 均未检测到横向溢出。

二次修复：

用户继续反馈：节点点开后内部内容仍看不全，部分文字被遮挡，Input / Output 区域像是丢失。

原因定位：

- 上一轮为了防止横向溢出，在 `.step`、`.stepBody`、`.stepDetail` 等展开路径上使用了 `overflow: hidden`，导致纵向详情被裁剪。
- `.stepList` 是纵向 flex 容器，`.step` 默认允许收缩；展开节点详情高度超过 `220px` 时，节点仍被压缩，详情会被后续节点压住。
- `.stepDetail code` 使用固定 `max-height` 和内部滚动，不符合当前阶段“点开后直接复核完整 Input / Output”的交互预期。

二次修复内容：

1. 移除 `.step`、`.stepBody`、`.stepDetail`、`.stepDetail > span` 上会裁剪纵向内容的 `overflow: hidden`。
2. `.step` 增加 `flex: 0 0 auto`，避免在 `.stepList` 内被压缩，展开后按详情真实高度撑开。
3. `.stepDetail code` 移除固定 `max-height` 与内部滚动，改为完整展示，由外层执行链列表统一滚动。
4. `.step b` 不再单行省略，改为可换行，避免节点标题被截断或遮挡。
5. 保留 `.stepList { overflow-x: hidden; }` 与各层 `min-width: 0` / `max-width: 100%` / `overflow-wrap: anywhere`，继续防止横向撑宽。

二次验证记录：

- `client` 执行 `npm run build` 通过，仅保留 Vite 大 chunk 常规提示。
- 浏览器重新触发同一黄金问题，稳定生成 8 个执行链节点。
- 逐个点开 8 个节点，均检测到 `工具`、`Input`、`Output` 三类详情标签。
- 逐个点开 8 个节点，`.step` 均按内容真实撑高，节点高度范围约 `327px` 到 `534px`，`clippedByStep=false`。
- 逐个点开 8 个节点，两个 `code` 块均未出现内部裁剪，`clippedCodes=false`。
- `.stepList` 与 `.step` 横向溢出均为 `0`。

## 11. 2026-05-21 P1 执行链 Input / Output 信息密度优化

本轮完成内容：

1. 后端新增 `ExecutionStep` schema，统一执行链节点字段为 `name`、`status`、`detail`、`tool`、`duration`、`input`、`output`。
2. 后端新增 `make_step` 构造入口，避免后续 mock 节点继续散落裸 `dict` 字段。
3. SQL Generator 输出统一改为 `generatedSql`。
4. SQL Sandbox 异常节点输入改为 `failedSql`、`validationRules`，输出补充 `repairReason`、`nextSkill`。
5. SQL Repair Skill 输入补充 `failedSql`、`repairReason`、`validationRules`，输出补充完整 `repairedSql`，移除“见下方最终 SQL 折叠面板”占位。
6. SQL 校验通过节点补充 `validatedSql`，SQL 执行节点补充 `executedSql`，便于按执行链逐节点复核。
7. 前端 `highlightJson` 移除 140 字符截断，避免 SQL、修复原因、校验规则在 Input / Output 中被悄悄截短。

涉及文件：

- `server/app/models/schemas.py`
- `server/app/mock/engine.py`
- `client/src/main.jsx`

验证记录：

- `server` 执行 `python -m compileall app` 通过。
- `client` 执行 `npm run build` 通过，仅保留 Vite 大 chunk 常规提示。
- 直接构造黄金问题 mock 结果，确认 SQL 三类节点字段为：
  - SQL Generator：`output.generatedSql`
  - SQL Sandbox：`input.failedSql`、`input.validationRules`、`output.repairReason`
  - SQL Repair Skill：`input.failedSql`、`input.repairReason`、`input.validationRules`、`output.repairedSql`

## 12. 2026-05-22 P1 Mock API 契约补全

本轮继续推进「Mock API 补全与契约稳定」，重点不是改 UI，而是把后续真实 API 替换前最容易漂移的接口协议收口成可查询、可验证的 mock 契约。

完成内容：

1. `StreamEvent` schema 增加 `sequence` 与 `emitted_at` 字段。
   - `sequence`：单次 SSE 流内从 1 开始递增。
   - `emitted_at`：后端发出事件时的 ISO-8601 UTC 时间。
   - 前端仍继续使用 `event.type` 与 `event.data`，新增字段为兼容增强，不影响现有渲染。
2. `stream_result` 统一通过内部 `make_event` 生成 SSE envelope，避免后续新增事件时继续手写散落字段。
3. 新增 `STREAM_EVENT_CONTRACT`，明确 `/api/chat/stream` 的 SSE 协议：`conversation_id`、`step`、`sql`、`answer`、`canvas`、`done`、`error`。
4. 新增 `MOCK_API_CONTRACT`，汇总第一阶段 mock 能力清单：会话、问答、SSE、数据资产、Skill 注册、画布 schema、下载契约、mock 搜索与 CSV 明细下载。
5. 后端新增配置接口：`GET /api/config/stream-contract`、`GET /api/config/mock-contract`。
6. 前端 API service 新增契约读取方法：`getStreamContract`、`getMockContract`。

涉及文件：

- `server/app/models/schemas.py`
- `server/app/mock/engine.py`
- `server/app/mock/catalog.py`
- `server/app/api/routes.py`
- `client/src/services/api.js`

验证记录：

```bash
cd server
python -m compileall app
```

结果：通过。

```bash
cd client
npm run build
```

结果：通过，仅保留 Vite 大 chunk 常规提示。

接口探测：

- `GET /api/config/stream-contract` 返回 200。
- `GET /api/config/mock-contract` 返回 200。
- `GET /api/config/data-assets` 返回 200。
- `GET /api/config/skills` 返回 200。
- `GET /api/mock/search?q=test` 返回 200。
- `POST /api/chat` 返回 200，包含 `conversation_id`、`intent`、`answer`、`visible_steps`、`sql`、`canvas`。
- `POST /api/chat/stream` 返回 200，前 3 条 SSE data 行可正常读取，首条 `conversation_id` 事件包含 `sequence: 1` 与 `emitted_at`。

当前 P1 状态更新：

- [x] 定义 SSE 事件协议：`conversation_id`、`step`、`sql`、`answer`、`canvas`、`done`、`error`。
- [x] 文档化画布 payload 协议，并通过 `GET /api/config/canvas-schema` 暴露。
- [x] 明确第一版下载继续使用 CSV，并通过 `GET /api/config/download-contract` 暴露。
- [x] 联网搜索 mock 固定结果已拆出到 `MOCK_SEARCH_RESULTS`，并通过 `GET /api/mock/search` 暴露。
- [x] 数据资产接口已返回表、字段、中文名、示例值、业务别名、指标口径。
- [x] Skill 注册接口已返回适用场景、触发条件、输入输出结构、MCP 工具、失败降级策略。
- [ ] 后续如需要对接真实服务，可继续把 `MOCK_API_CONTRACT` 拆成 OpenAPI examples 或独立 markdown 契约文档。

## 13. 2026-05-23 P2 真实 API 替换准备：会话存储迁移结构

本轮开始 P2「真实 API 替换准备」，按清单第一项先处理会话接口底层存储。现有会话 API 行为保持不变，重点把 SQLite mock 从启动时硬编码建表升级为可迁移结构，方便后续切换正式数据库或迁移更多字段。

完成内容：

1. `server/app/storage.py` 新增版本化 migration 机制。
   - 新增 `schema_migrations` 元数据表。
   - 新增 `MIGRATIONS` 列表，当前包含 2 个版本。
   - `init_db()` 改为调用 `migrate_db()`，启动时按版本顺序补齐 schema。
2. 当前 schema version 为 2。
   - v1：创建 `conversations` 与 `messages` 基础表。
   - v2：补充 `conversations.pinned`，并新增会话列表与消息查询索引。
3. 保留旧库兼容能力。
   - 如果旧 SQLite 已经存在 `pinned` 字段，migration 会跳过重复 ALTER。
   - 旧库首次启动会补写 migration 记录。
4. 新增环境变量 `CHATBI_DB_PATH`，允许本地、测试和后续部署切换 SQLite 文件路径。
5. 新增 `GET /api/config/storage`，返回当前存储引擎、数据库路径、schema version 和已应用 migrations，便于验收。
6. 更新 `server/README.md`，补充存储迁移说明和 `CHATBI_DB_PATH` 用法。

涉及文件：

- `server/app/storage.py`
- `server/app/api/routes.py`
- `server/app/mock/catalog.py`
- `server/README.md`
- `history/codex_后续工作_20260521.md`

验证记录：

```bash
cd server
python -m compileall app
```

结果：通过。

迁移 smoke test：

- 使用独立临时 SQLite 文件执行 `init_db()`。
- 确认 `schema_version == 2`。
- 创建会话、追加问答消息、置顶会话、读取列表与详情均通过。
- 测试结束后删除临时 SQLite 文件。

接口探测：

- `GET /api/config/storage` 返回 200。
- 返回内容包含 `engine: sqlite`、`schema_version: 2`、`available_migrations: 2` 和 migrations 列表。

当前 P2 状态更新：

- [x] 会话接口：SQLite mock 存储升级为可迁移存储结构。
- [ ] 数据资产 / 字段口径：从 mock 配置升级为 YAML/JSON 或数据库配置。
- [ ] Dify 知识库检索：先封装 Retrieve API，再封装为 MCP。
- [ ] MySQL 查询：只读 SELECT，增加 SQL 安全校验与 allowed tables 过滤。
- [ ] Tavily MCP 搜索：替换固定 mock 搜索结果。
- [ ] LangGraph Agent：替换当前硬编码 intent/mock engine。
- [ ] 权限系统：MVP 后补充用户角色、可访问场景、可访问表、可下载字段。

## 14. 2026-05-23 P2 真实 API 替换准备：数据资产配置外置

本轮继续 P2 第二项：将数据资产 / 字段口径从后端硬编码 mock 常量升级为外部 JSON 配置。目标是先建立可替换的配置入口，后续可平滑改为 YAML、数据库配置表或后台管理页面。

完成内容：

1. 新增 `server/config/data_assets.json`。
   - 包含 `version`、`description`、`assets`、`metric_definitions`。
   - 当前收录 8 张 mock 数据资产表与 2 个指标口径。
   - 字段继续覆盖英文名、中文名、类型、示例值、业务别名和指标路径。
2. 新增 `server/app/config_loader.py`。
   - `load_data_catalog()` 默认读取 `server/config/data_assets.json`。
   - 支持环境变量 `CHATBI_DATA_ASSETS_PATH` 覆盖配置路径。
   - 配置文件缺失、JSON 无效或结构不符合预期时，回退到 `app/mock/catalog.py` 中的内置 mock 常量。
3. `GET /api/config/data-assets` 改为通过 loader 返回配置。
   - 返回中新增 `version`、`source`、`path`，便于确认当前实际配置来源。
   - 保留 `assets` 与 `metric_definitions` 字段，不破坏前端现有消费方式。
4. `MOCK_API_CONTRACT` 中的数据资产能力补充配置来源和覆盖环境变量。
5. 更新 `server/README.md`，补充 `config/data_assets.json` 与 `CHATBI_DATA_ASSETS_PATH` 说明。

涉及文件：

- `server/config/data_assets.json`
- `server/app/config_loader.py`
- `server/app/api/routes.py`
- `server/app/mock/catalog.py`
- `server/README.md`
- `history/codex_后续工作_20260521.md`

验证记录：

```bash
cd server
python -m compileall app
```

结果：通过。

配置文件校验：

- `server/config/data_assets.json` 可被 Python `json.loads` 正常解析。
- 当前 `version` 为 `2026-05-23.mock.v1`。
- 当前包含 8 个 `assets`、2 个 `metric_definitions`。
- 抽样确认字段中文名、业务别名、指标公式存在。

接口探测：

- `GET /api/config/data-assets` 返回 200。
- 返回 `source: json`、`version: 2026-05-23.mock.v1`。
- 返回 assets 与 metric_definitions 数量符合预期。

环境变量覆盖探测：

- 使用 `CHATBI_DATA_ASSETS_PATH` 指向临时 JSON 文件。
- `load_data_catalog()` 成功读取 override 配置，返回 `version: override-probe`、`table: probe_table`。
- 探测完成后已删除临时 override 文件。

当前 P2 状态更新：

- [x] 会话接口：SQLite mock 存储升级为可迁移存储结构。
- [x] 数据资产 / 字段口径：从 mock 配置升级为 YAML/JSON 或数据库配置。
- [ ] Dify 知识库检索：先封装 Retrieve API，再封装为 MCP。
- [ ] MySQL 查询：只读 SELECT，增加 SQL 安全校验与 allowed tables 过滤。
- [ ] Tavily MCP 搜索：替换固定 mock 搜索结果。
- [ ] LangGraph Agent：替换当前硬编码 intent/mock engine。
- [ ] 权限系统：MVP 后补充用户角色、可访问场景、可访问表、可下载字段。

## 15. 2026-05-25 P2 真实 API 替换准备：Dify Retrieve 与 `.env` 配置入口

本轮继续 P2 第三项：先完成真实 Dify 知识库 Retrieve API 的可替换边界，并把真实 API 连接信息统一收口到 `.env`。当前目标不是大改 UI，而是让后端可以在 mock fallback 与真实 Dify 之间平滑切换。

完成内容：

1. 新增 `server/app/dify.py`。
   - 集中封装 Dify dataset 映射、Retrieve payload 构造、真实请求、mock fallback 和返回结构归一化。
   - 支持 `semantic_search`、`keyword_search`、`hybrid_search`、分数阈值、Rerank 与混合检索权重。
2. 新增 `KnowledgeRetrieveRequest` / `KnowledgeRetrieveResult` schema。
3. 新增后端接口：
   - `GET /api/config/dify`
   - `POST /api/knowledge/retrieve`
4. 前端 `client/src/services/api.js` 新增：
   - `getDifyConfig`
   - `retrieveKnowledge`
5. 新增 `server/app/settings.py`，统一读取项目根目录 `.env`，并兼容 `server/.env`。
6. 新增 `.env.example` 作为安全模板，真实 `.env` 已加入 `.gitignore`。
7. 根据 `DIFY/DIFY_知识库_API集成文档.md` 填入本地 `.env`：
   - `CHATBI_DIFY_ENABLED=true`
   - `DIFY_API_BASE_URL=http://10.30.11.215:9879`
   - `DIFY_API_KEY` 已填写到本地 `.env`，不写入交接文档。
   - Rerank 与 Embedding 模型配置沿用文档推荐值。

涉及文件：

- `.gitignore`
- `.env.example`
- `.env`（本地忽略文件，不提交）
- `server/app/settings.py`
- `server/app/dify.py`
- `server/app/models/schemas.py`
- `server/app/api/routes.py`
- `server/app/mock/catalog.py`
- `server/app/config_loader.py`
- `server/app/storage.py`
- `server/requirements.txt`
- `server/README.md`
- `client/src/services/api.js`

验证记录：

```bash
cd server
python -m compileall app
```

结果：通过。

```bash
cd client
npm run build
```

结果：通过，仅保留 Vite 大 chunk 常规提示。

配置探测：

- `GET /api/config/dify` 返回 `provider: dify`。
- `base_url` 返回 `http://10.30.11.215:9879`。
- `api_key_configured` 返回 `true`。
- `.env` 在 `git status --ignored` 中显示为 ignored。

当前 P2 状态更新：

- [x] 会话接口：SQLite mock 存储升级为可迁移存储结构。
- [x] 数据资产 / 字段口径：从 mock 配置升级为 YAML/JSON 或数据库配置。
- [x] Dify 知识库检索：已封装 Retrieve API、mock fallback 和 `.env` 配置入口，下一步验证真实 Dify 返回结构并视情况修正归一化逻辑。
- [ ] MySQL 查询：只读 SELECT，增加 SQL 安全校验与 allowed tables 过滤。
- [ ] Tavily MCP 搜索：替换固定 mock 搜索结果。
- [ ] LangGraph Agent：替换当前硬编码 intent/mock engine。
- [ ] 权限系统：MVP 后补充用户角色、可访问场景、可访问表、可下载字段。

补充验证：

1. 使用真实 Dify dataset id `ffa84ba6-4ec9-44a0-8f6d-594b27f7a829` 调用 `POST /api/knowledge/retrieve`。
2. `semantic_search` 返回 `provider: dify`，records 数量符合 `top_k`，无 fallback。
3. `hybrid_search + reranking_enable=true` 返回 `provider: dify`，payload 中包含 `reranking_model`，无 fallback。
4. 发现当前 Dify 服务对 `httpx` 请求会触发连接重置，但 Python 标准库 `http.client` 可稳定返回 200，因此 `server/app/dify.py` 已改为使用标准库 HTTP 客户端。
5. 中文 dataset 名称在 PowerShell 管道测试中会被本地控制台编码影响，接口真实调用建议优先传 dataset id；浏览器/HTTP JSON 正常 UTF-8 请求不受该测试管道影响。

真实 Dify 测试用例与返回结果：

1. 语义检索测试：

```json
{
  "query": "stock days",
  "dataset_id": "ffa84ba6-4ec9-44a0-8f6d-594b27f7a829",
  "search_method": "semantic_search",
  "top_k": 2,
  "score_threshold_enabled": false,
  "reranking_enable": false
}
```

返回摘要：

- `status: 200`
- `provider: dify`
- `dataset_id: ffa84ba6-4ec9-44a0-8f6d-594b27f7a829`
- `records: 2`
- `fallback: None`
- 前两条命中内容分别为 2024 年 2 月总库存相关 SQL 问答、2024 年 2 月库龄 30 天以内车辆数相关 SQL 问答。
- 返回分数约为 `0.4053`、`0.4028`。

2. 混合检索 + Rerank 测试：

```json
{
  "query": "stock days",
  "dataset_id": "ffa84ba6-4ec9-44a0-8f6d-594b27f7a829",
  "search_method": "hybrid_search",
  "top_k": 2,
  "score_threshold_enabled": false,
  "reranking_enable": true
}
```

返回摘要：

- `status: 200`
- `provider: dify`
- `records: 2`
- `fallback: None`
- `payload_has_reranking_model: True`

结论：

- 真实 Dify API 已经连通。
- `semantic_search` 与 `hybrid_search + rerank` 都可用。
- 后端没有走 mock fallback。
- PowerShell 终端直接打印中文内容时可能乱码，但 Dify 原始响应为 Unicode JSON，接口层可正常解析。

当前 P2 状态再次更新：

- [x] 会话接口：SQLite mock 存储升级为可迁移存储结构。
- [x] 数据资产 / 字段口径：从 mock 配置升级为 YAML/JSON 或数据库配置。
- [x] Dify 知识库检索：真实 Retrieve API 已连通，semantic 与 hybrid+rerank 路径均验证通过。
- [ ] MySQL 查询：只读 SELECT，增加 SQL 安全校验与 allowed tables 过滤。
- [ ] Tavily MCP 搜索：替换固定 mock 搜索结果。
- [ ] LangGraph Agent：替换当前硬编码 intent/mock engine。
- [ ] 权限系统：MVP 后补充用户角色、可访问场景、可访问表、可下载字段。

## 16. 2026-05-25 P2 真实 API 替换准备：MySQL Query Service 与 SSH 隧道配置

本轮开始 P2 第四项：在接真实 MySQL 前，先补齐 Query Service 外壳、SQL 安全校验与 SSH 隧道配置入口。考虑到真实数据库需要通过 SSH 才能访问，项目侧新增了自动 SSH tunnel 能力，用户后续只需要把 SSH 与 MySQL 信息填入本地 `.env`，无需手动在 VSCode 外维护隧道。

完成内容：

1. `.env.example` 新增 Query Provider、MySQL、SSH tunnel 配置字段：
   - `CHATBI_QUERY_PROVIDER`
   - `MYSQL_HOST` / `MYSQL_PORT` / `MYSQL_USER` / `MYSQL_PASSWORD` / `MYSQL_DATABASE`
   - `MYSQL_SSH_ENABLED`
   - `MYSQL_SSH_HOST` / `MYSQL_SSH_PORT` / `MYSQL_SSH_USER`
   - `MYSQL_SSH_PASSWORD`
   - `MYSQL_SSH_PRIVATE_KEY_PATH`
   - `MYSQL_SSH_PRIVATE_KEY_PASSPHRASE`
   - `MYSQL_SSH_LOCAL_PORT`
2. `server/app/settings.py` 新增上述配置读取，所有敏感字段只在服务端内部使用。
3. 新增 `server/app/sql_guard.py`：
   - 只允许单条 `SELECT`。
   - 拦截 `INSERT`、`UPDATE`、`DELETE`、`DROP`、`ALTER`、`TRUNCATE`、`GRANT`、`OUTFILE` 等危险关键字或模式。
   - 从 `FROM` / `JOIN` 提取表名。
   - 使用 `GET /api/config/data-assets` 背后的数据资产表作为 allowed tables。
4. 新增 `server/app/query_service.py`：
   - 默认 `mock` provider。
   - 当 `CHATBI_QUERY_PROVIDER=mysql` 时，使用 PyMySQL 查询真实 MySQL。
   - 当 `MYSQL_SSH_ENABLED=true` 时，使用 `sshtunnel` 在查询期间自动建立 SSH 隧道。
   - 配置接口只返回“是否已配置”，不回显密码、私钥、连接串。
5. 新增后端接口：
   - `GET /api/config/query`
   - `POST /api/query/validate`
   - `POST /api/query/execute`
6. `server/README.md` 补充 Query Service 与 SSH tunnel 配置说明。

涉及文件：

- `.env.example`
- `server/app/settings.py`
- `server/app/sql_guard.py`
- `server/app/query_service.py`
- `server/app/models/schemas.py`
- `server/app/api/routes.py`
- `server/requirements.txt`
- `server/README.md`
- `history/codex_后续工作_20260521.md`

验证记录：

```bash
cd server
python -m compileall app
```

结果：通过。

安全用例：

- 合法 `SELECT area_name, model_name, terminal_qty FROM v_dm_sal_wolesale_terminal_dly LIMIT 2` 校验通过。
- `DELETE FROM v_dm_sal_wolesale_terminal_dly` 被拦截，返回非 SELECT 与危险关键字错误。
- `SELECT * FROM forbidden_table` 被拦截，返回非 allowed tables 错误。
- `POST /api/query/execute` 在默认 mock provider 下返回 2 条样例行。
- `GET /api/config/query` 当前返回 `provider: mock`，MySQL 与 SSH 仍未配置。

下一步：

- 用户将真实 MySQL 与 SSH 信息填入本地 `.env`。
- 将 `CHATBI_QUERY_PROVIDER=mysql`、`MYSQL_SSH_ENABLED=true` 后，执行 `SELECT 1` 与一条白名单表查询做真实连通验证。
- 连通后再把 mock engine 中的 `mock-mysql-query` 替换为 Query Service 调用。

补充验证：

1. 用户已将 SSH 与 MySQL 信息填入本地 `.env`。
2. 清理 `.env` 中非 `KEY=value` 的说明行，避免 SSH 密码以普通文本说明形式残留；保留结构化环境变量。
3. 将本地 `.env` 切到：
   - `CHATBI_QUERY_PROVIDER=mysql`
   - `MYSQL_SSH_ENABLED=true`
4. 安装新增依赖 `sshtunnel`、`pymysql`；发现 `sshtunnel 0.4.0` 与 `paramiko 5.x` 不兼容，已将 `server/requirements.txt` 补充 `paramiko<4` 并安装 `paramiko 3.5.1`。
5. `POST /api/query/execute` 执行 `SELECT 1 AS ok` 成功：
   - `provider: mysql`
   - `row_count: 1`
   - `columns: ["ok"]`
   - `rows: [{"ok": 1}]`
6. 白名单业务表 `v_dm_sal_wolesale_terminal_dly` 查询失败，真实库返回 unknown table。
7. 诊断当前数据库：
   - 当前数据库为 `ads_aiwork`
   - `information_schema.tables` 中当前只查到 `test` 表
   - `test` 表 `COUNT(*)` 为 0

当前判断：

- SSH tunnel、MySQL 登录、PyMySQL 查询链路已经打通。
- 当前阻塞不是连接问题，而是真实数据库 `ads_aiwork` 中还没有 mock 配置里的 8 张业务表 / 视图。
- 下一步需要确认真实业务库名、schema/catalog 或真实表名，然后更新 `server/config/data_assets.json` 中的表名与字段配置。

当前 P2 状态更新：

- [x] 会话接口：SQLite mock 存储升级为可迁移存储结构。
- [x] 数据资产 / 字段口径：从 mock 配置升级为 YAML/JSON 或数据库配置。
- [x] Dify 知识库检索：真实 Retrieve API 已连通，semantic 与 hybrid+rerank 路径均验证通过。
- [x] MySQL 查询：只读 SELECT、allowed tables 校验、危险 SQL 拦截与 Query Service 外壳已完成；真实 SSH + MySQL 已连通，待确认真实业务表配置。
- [ ] Tavily MCP 搜索：替换固定 mock 搜索结果。
- [ ] LangGraph Agent：替换当前硬编码 intent/mock engine。
- [ ] 权限系统：MVP 后补充用户角色、可访问场景、可访问表、可下载字段。
